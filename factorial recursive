#include <stdio.h>
int fact(int n)
{
    if(n!=1)
    {
        return n*fact(n-1);
    }
    return 1;
}

int main()
{
    int n;
    printf("Enter a value: " );
    scanf("%d",&n);
    int factorial=fact(n);
    printf("Factorial of %d is %d",n,factorial);
}

