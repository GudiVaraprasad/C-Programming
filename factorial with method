#include <stdio.h>
int fact(int n)
{
    int i;
    int fact=1;
    for(i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    return fact;
}

int main()
{
    int n;
    printf("Enter a value: " );
    scanf("%d",&n);
    int factorial=fact(n);
    printf("Factorial of %d is %d",n,factorial);
}

